install.packages("BetaBit")
library(BetaBit)
proton()
employees[employees$name=='John' & employees$surname=='Insecure',]
proton(action = "login", login="johnins")

for( i in 1:length(top1000passwords))
{
haslo<-top1000passwords[i]
if(proton(action = "login", login="johnins", password=haslo)=='Success! User is logged in!')
{
  print(haslo)
  break
}
}

"q1w2e3r4t5"
library(dplyr)

employees[employees$surname=='Pietraszko',]

logs[logs$login=="slap",] 
  group_by_(host) <- summarie(n())
logs[logs$login=="slap",] %>%  
group_by(host) %>% 
  summarise(avg = n())-> s
s
proton(action = "server", host="194.29.178.16")

bash_history



for( i in 1:length(top1000passwords))
{
  haslo<-top1000passwords[i]
  if(proton(action = "login", login="slap", password=haslo)=='Success! User is logged in!')
  {
    print(haslo)
    break
  }
}

for(i in 1:length(bash_history))
{
  
  for (l in 1:length(strsplit(bash_history[i],' ')[[1]]))
     {
      haslo=strsplit(bash_history[i],' ')[[1]][l]
        
        if(proton(action = "login", login="slap", password=haslo)=='Success! User is logged in!')
        {
          print(haslo)
          break
        }
      }

}

"DHbb7QXppuHnaXGN"


